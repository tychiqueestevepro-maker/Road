# Optional. Leave unset for a single Vercel project so the app uses same-origin /api routes.
# NEXT_PUBLIC_API_URL=https://verytis.com
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=
NEXT_PUBLIC_MAPBOX_STYLE_URL=mapbox://styles/mapbox/streets-v12
